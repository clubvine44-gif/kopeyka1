/* KOPEYKA FIXES — calculation corrections + authenticated cloud bootstrap */
(function(){'use strict';
const n=v=>Number(v)||0,today=()=>new Date().toISOString().slice(0,10);
const original=window.computePeriodSummary;
function incomeFor(start,end){let actual=0,expected=0;(window.STATE?.income||[]).forEach(i=>{if(!i||i.date<start||i.date>end)return;const a=n(i.amount);if(['expected','planned','forecast'].includes(i.status))expected+=a;else actual+=a});return{actual,expected,total:actual+expected}}
function correctedSummary(state,start,end){const base=original?original(state,start,end):{},inc=incomeFor(start,end),t=today();let ar=0,pr=0,ao=0,po=0;(state.expenses||[]).forEach(e=>{if(!e||e.date<start||e.date>end)return;const a=n(e.amount);if(e.mandatory)e.date>t?po+=a:ao+=a;else e.date>t?pr+=a:ar+=a});(state.recurring||[]).filter(r=>r.active).forEach(r=>{try{occurrencesInRange(r,start,end).forEach(d=>{const a=n(r.amount);d>t?pr+=a:ar+=a})}catch(_){}});let reservesNeeded=0;(state.reserves||[]).filter(r=>r.active).forEach(r=>{try{reservesNeeded+=n(reserveRawNeed(r,start,end,inc.total))}catch(_){}});const balance=n(state.settings?.currentBalance),available=Math.max(0,balance+inc.actual-ar-ao),forecast=Math.max(0,balance+inc.actual+inc.expected-ar-ao-pr-po),safe=Math.max(0,forecast-reservesNeeded),strict=Math.max(0,balance+inc.actual-ar-ao-pr-po-reservesNeeded),remaining=Math.max(1,diffDays(t>start?t:start,end)+1);return Object.assign({},base,{periodStart:start,periodEnd:end,actualIncome:inc.actual,expectedIncome:inc.expected,totalIncome:inc.total,currentBalance:balance,availableNow:available,forecastBeforeReserves:forecast,forecastAfterReserves:safe,strictAvailable:strict,noReserveLimit:Math.round(forecast/remaining),safeLimit:Math.round(safe/remaining),strictLimit:Math.round(strict/remaining),actualRegular:ar,plannedRegular:pr,totalRegular:ar+pr,actualObligatory:ao,plannedObligatory:po,totalObligatory:ao+po,reservesNeeded})}
window.computePeriodSummary=correctedSummary;
try{window.SHIFT_RATE=0;window.MONTHLY_INCOME=0}catch(_){}
/* Legacy transaction-based cloud sync is intentionally removed. The authenticated cloud module is loaded here because this file is already part of the app's script chain. */
function loadAuthCloud(){if(window.kopeykaCloud||document.querySelector('script[data-kopeyka-auth]'))return;const s=document.createElement('script');s.src='./auth-cloud.js?v=4';s.async=false;s.dataset.kopeykaAuth='1';s.onload=()=>console.log('Kopeyka: authenticated cloud ready');s.onerror=e=>console.error('Kopeyka: auth-cloud load failed',e);document.head.appendChild(s)}
loadAuthCloud();
})();